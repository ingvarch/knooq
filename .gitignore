# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
dev-debug.log

# Dependency directories
node_modules/

# Environment variables
.env

# Editor directories and files
.idea
.vscode
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# OS specific
.DS_Store
.taskmaster

# Xcode — project.yml is the source of truth (XcodeGen), but the generated
# .xcodeproj IS committed so Xcode Cloud / fresh clones build with no extra setup.
# Regenerate with `xcodegen generate` after editing project.yml, then commit.
xcuserdata/
DerivedData/
.swiftpm/

